use Mix.Config

# In this file, we keep production configuration that
# you likely want to automate and keep it away from
# your version control system.
config :slack,
  token: "your-slack-token-here"
  owner: "owner-of-the-bots-nickname"
